{
  "name": "tina4-nodejs",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "This is not a framework. Tina4 for Node.js/TypeScript.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist",
    "test": "tsx test/run-all.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "esbuild": "^0.24.0"
  }
}
