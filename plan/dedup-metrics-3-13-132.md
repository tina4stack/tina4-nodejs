# Task: Behavior-preserving DEDUP (metrics audit) — tina4-nodejs

Outcome: unify 3 metrics-flagged duplicate blocks with zero behavior change, proven by the
existing test runner + typecheck staying green and a stashed-baseline +0-failure diff.
Branch: feature/release3.13.132 (no new branch). No push/tag/version bump.

## Scope
- [x] #3 cli/src/commands/generate.ts:2395 — 62 lines x2. **SKIP** — twin is emitQueueTest; the two share only the
      already-extracted `writeTest(slug, standaloneTest(doc, body))` wrapper (generate.ts:2278/2291). Their doc/body
      are entirely different bespoke test-generator strings (middleware-dispatch vs queue-job). Structural token
      match, not shared logic; unifying would move each unique body to its call site (0 lines saved) and risks
      changing verified generated bytes.
- [x] #4 orm/src/adapters/firebird.ts:908 — 30 lines x3 (firebird/mssql/mysql). **SKIP** — `fieldTypeToX` is a
      deliberate per-adapter DDL type mapper; ALL 7 adapters have one (mysql/postgres/sqlite/mongodb/firebird/mssql/
      odbc). Every switch branch returns a DIFFERENT dialect type (boolean→INTEGER/TINYINT(1)/BIT; text→BLOB SUB_TYPE
      TEXT/TEXT/NTEXT; number→DOUBLE PRECISION/DOUBLE/FLOAT; VARCHAR vs NVARCHAR). Only common part is the switch
      skeleton; extracting it flattens toward a per-engine data table (net-neutral LOC), couples independent adapters,
      and changes the create-table DDL path across 3 engines whose real tests are lab-only (unverifiable no-mock here).
- [x] #6 frond/src/engine.ts:3214 — 22 lines x3. **UNIFIED** — extracted `collectBlockBody(tokens, start, openTag,
      closeTag, canNest?)`; applied to handleCache/handleSpaceless/handleAutoescape (identical) + handleSetBlock
      (inline-`set` guard via canNest). Net −48 lines; offender + nested 10x4/8x3 sub-clusters gone.
- [x] DID NOT TOUCH: mongoClient.ts, gallery/*, modelCollection.ts/baseModel.ts.

## Metrics (duplication line, `tina4 metrics --path packages`)
- BEFORE: `duplication: 104 repeated blocks   1668 lines removable by unifying them`
- AFTER:  `duplication: 102 repeated blocks   1594 lines removable by unifying them`  (−2 blocks, −74 removable lines)

## Tests (real, no mocks)
- [x] `npm run typecheck` exit 0 (with change)
- [x] `npx tsx test/run-all.ts`: 8134 passed, 33 failed, 233 skipped (all fail/skip = missing live services on macOS)
- [x] stashed-baseline vs applied: BYTE-IDENTICAL (8134/33/233, same 27 failed files) → +0 failures, +0 skips
- [x] frond suites exercising the 4 handlers all green: frond(297) frondtags(5) frondCacheBounds(18)
      frondTemplateCache(22) frondExpressionParity(130) frondSandboxContract(22) frond-import-as(8)

## Bugs
- (none — pure refactor, zero behavior change proven by stashed-baseline diff)

## Commits
- (hash  frond: extract collectBlockBody helper — dedup 4 block-body loops)

## Status: Complete
