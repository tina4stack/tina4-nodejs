# Combined Node PR integration — 2026-09-24

Outcome: integrate open Node changes into v3, preserve each PR head as an ancestor, then run the combined lab suite per maintainer order.

## Scope
- [x] Merge #69 transport/browser gate, #70 CI, #73 SQL/CORS/security headers, #74 optional drivers/mail/SOAP, #75 Frond escaping, #76 request limits/content type, #77 skill estimates, #78 scratch isolation, #79 generated ORM keys.
- [x] Resolve #69/#73 security middleware and #69/#76 request-limit interaction.
- [x] Fix CodeQL polynomial whitespace scan and test assertions/logging/file access.
- [x] Fix API reflection scanner handling of regexp quotes.
- [ ] Required GitHub CI + CodeQL pass on combined head.
- [ ] Merge and remove included remote/local branches after verifying merge.
- [ ] Combined lab suite after integration (requested order).

## Verification
- Combined tree: typecheck, API docs21, security headers14 passed.
- Targeted checks during integration: SQL translator62, CORS23, HTTP transport22, upload/content type10, Frond escaping15, scratch-path detector15 passed.
- Driverless tree compiles with zero optional packages resolvable. Zero-dependency install53pass/3fail locally: PostgreSQL role tina4 absent; Mongo/Redis paths passed. CI/lab must verify PostgreSQL.
- Full lab suite has not been run here.

## Commit handling
Original PR heads retained as local audit/prN-original refs until merge verification. PR consolidations signed by configured committer; no original-author signatures fabricated. Combined PR uses merge commits preserving all current PR heads.

## Combined CodeQL follow-up
- [x] CORS origin checks use explicit token equality (CodeQL still classified array.includes as a URL substring check).
- [x] Browser gate diagnostics keep the same w+ file descriptor through fstat/readSync with an explicit tail offset; no path reopen.
- [x] Typecheck, 23 CORS checks, and 9 real-server browser gate checks passed.
- [ ] Replacement combined CI / CodeQL.

## Dedicated Firebird coverage fix
- Main CI at ca64234: 10,131 passed / 1 failed because ORM read-back test unconditionally rejected the main job's intentionally absent Firebird URL.
- [x] Reuse shared tagged-service skip policy: an absent optional engine is excused; a configured unreachable engine remains a failure.
- [x] Add --engine selection and explicitly run ORM read-back in live Firebird job, rejecting any skip or zero passing cases.
- [x] SQLite read-back17, shared service gate47, typecheck passed locally; workflow YAML parsed.
- [ ] Replacement full main CI and dedicated real-Firebird coverage.

## Statement Firebird coverage
- Main CI at 81aeb4c: 10,199 passed, zero failed assertions/files; strict service audit rejected three untagged optional Firebird skips. Dedicated ORM Firebird passed17.
- [x] Tag optional database reasons in the inherited statement contracts; preserve ODBC and required-service failures.
- [x] Run execute rows, fetch-write and placeholder contracts explicitly against live Firebird in its dedicated job, rejecting any skip or zero assertions.
- [x] SQLite focused contracts47 assertions, shared gate47, typecheck and YAML syntax pass locally.
- [ ] Replacement full main CI and dedicated statement Firebird execution.

## MongoDB 8 independent review
- [x] Set explicit session Mongo host/port to27018; zero-dependency fallback constructs host/port handlers and otherwise tests Mongo7 despite the URI.
- [x] Reject silent skips in the Mongo8 loop. Selected session/cache suites also use Redis/Valkey/Memcached already required and provisioned in this job; no optional-engine skips are intentionally allowed.
- [ ] Hosted live replica verification.
